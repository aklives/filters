run make filter to compile a binary for your OS

run ./filter -g, -s, -r, -b inputfile.bmp outputfile.bmp

(grayscale, sepia, reflect, and blur)

will create a new version of the original BMP file

feel free to use the samples in the images folder
